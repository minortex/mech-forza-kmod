# Allow trusted local administrators to use the EC bridge without running the
# userspace controller as root. Do not make this device world-writable.
SUBSYSTEM=="misc", KERNEL=="mechrevo-ec", GROUP="wheel", MODE="0660"
